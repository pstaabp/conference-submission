extends layout

block proprow
  tr
    td.header #{time}
    each proposal in props
      - var authors = "";
      - var session = "";
      - var title = "";
      - if(proposal) {
      -    authors = proposal.author;
      -    proposal.other_authors.forEach(function(auth){ 
      -      if(auth && auth.first_name && auth.first_name.length>0){
      -        authors += ", " + auth.first_name + " " + auth.last_name;
      -      }});
      -    session = proposal.session;
      -    title = proposal.title; 
      -    id = proposal._id;
      - }
      td.op-prop 
        
        a(href!="/#{top_dir}/proposals/#{id}") #{title}
        span.op-author 
          | #{authors}

block container
  h2 Oral Presentation Schedule

  table.table.table-bordered
    thead
      tr
        th Time
        th Hamm. G01
        th Hamm. G01B
        th G19
    tbody
      - var times = ["9:00-9:15","9:20-9:35","9:40-9:55","10:00-10:15","","11:00-11:15","11:20-11:35","11:40-11:55"];
      - for (var i = 0; i<10 ; i++) {
      - var time = times[i];
      -   var props =[];
      -   for(var j = 0; j<3; j++) { props.push(proposals[6*i+j]); } 
      block proprow
      - }
      
      



  
